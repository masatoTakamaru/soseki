<%= provide :title, t("title.item_master.index") %>

<div class="container">

<h2><%= t("title.item_master.index") %></h2>

<div class="flex justify-end">
  <%= link_to t(".new"), new_item_master_path, method: "get", class: "btn py-2 px-8" %>
</div>

<% if @item_masters.empty? %>
  <p>講座が登録されていません。「新規登録」をクリックして最初の講座を登録して下さい。</p>
<% end %>


</div>